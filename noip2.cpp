#include <iostream>
#include <math.h>

using namespace std;
void prime(int n);
int main()
{
    int n, k;
    cin >> n >> k;
    prime(n);

}
void prime(int n){
    for(int i = 2; i < n; i++)
    for(int j = 2; j < i; j++)
    {
        
    }
    
    
}